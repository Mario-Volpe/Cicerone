<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.cicerone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".DettagliFeedback"></activity>
        <activity android:name=".GestioneRichiesta" />
        <activity android:name=".DettaglioRichieste" />
        <activity android:name=".DettagliAttivita" />
        <activity android:name=".ElencoAttivita" />
        <activity android:name=".Creazione" />
        <activity android:name=".FormRicerca" />
        <activity android:name=".Cerca" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".RegistrationActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ElencoFeedback" />

        <!--suppress AndroidDomInspection -->
        <service android:name=".data.model.SendIt" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>